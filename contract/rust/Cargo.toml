[package]
name = "cerberus-contract"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Frozen v0.1 integration contract types for Cerberus (Rust side)."

[dependencies]
serde = { workspace = true }
serde_json = "1"
