version: v2
# v0.1: codegen optional — contract/go and contract/rust are handwritten mirrors (see CONTRACT.md).
# When buf + plugins are installed, `buf generate` (run from proto/) emits Go bindings.
plugins:
  - remote: buf.build/protocolbuffers/go
    out: ../contract/go/gen
    opt:
      - paths=source_relative
