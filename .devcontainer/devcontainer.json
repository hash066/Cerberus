{
  "name": "Cerberus Platform",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "remoteUser": "vscode",
  "customizations": {
    "vscode": {
      "extensions": [
        "golang.Go",
        "rust-lang.rust-analyzer",
        "task.vscode-task"
      ]
    }
  },
  "postCreateCommand": "go version && rustc --version && cargo --version && task --version && goreleaser --version"
}
