# Go
/bin/
*.exe
*.test
*.out
go.work.sum

# Rust
/target/
**/target/
Cargo.lock.orig

# WASM components
components/**/*.wasm
!components/examples/**/fixtures/*.wasm

# Tauri / node
tray/node_modules/
tray/dist/
tray/src-tauri/target/
# bundled cerberusd sidecar — built at (before-)build time by tray/build-sidecar.mjs
tray/src-tauri/binaries/

# codegen output (regenerated by `task gen`)
contract/go/gen/
contract/rust/gen/

# editors / os
.idea/
.vscode/
.DS_Store
Thumbs.db

# local runtime
*.sock
/.cerberus/
.cerberus-e2e-*/
*.log

# Claude Code local settings + agent worktrees (ephemeral)
.claude/
